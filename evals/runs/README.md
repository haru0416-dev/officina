# Evaluation Runs

This directory contains generated outputs from local evaluation runs.

The files are not part of the runtime Skill. They are included as
evidence artifacts so readers can inspect how the package was tested.

Current run:

- `generic-comparison.md`: manual scorecard of baseline vs terse vs
  Officina on `evals/fixtures/generic-vs-primitive.md`, using
  `evals/value-metrics.md`.

The saved comparison is deliberately small. It supports only the
narrow claim stated in `generic-comparison.md`; it is not a benchmark.
